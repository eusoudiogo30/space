{
  "name": "buraco-doido",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB,ADMIN,SPACE -c green,cyan,magenta,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\" \"npm run dev --prefix space-adventure/frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix admin && npm run build --prefix space-adventure/frontend",
    "build:app": "npm run prisma:generate --prefix backend && npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix admin && npm run build --prefix space-adventure/frontend",
    "start": "npm run start --prefix backend",
    "typecheck": "npm run typecheck --prefix backend && npm run build --prefix frontend && npm run typecheck --prefix admin && npm run build --prefix space-adventure/frontend",
    "test": "npm run test --prefix backend && npm run test --prefix admin",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin && npm install --prefix space-adventure/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
